<UserControl
    x:Class="ClipboardCanvas.UserControls.CanvasDisplay.WebViewCanvasControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ap="using:ClipboardCanvas.AttachedProperties"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:ClipboardCanvas.UserControls.CanvasDisplay"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Spacing="12">
            <TextBlock Text="{x:Bind ViewModel.Source, Mode=OneWay}" />
            <WebView
                x:Name="ContentWebView"
                ap:WebViewNavigationAttachedProperty.Value="{x:Bind ViewModel.TextHtml, Mode=OneWay}"
                x:Load="{x:Bind ViewModel.ContentWebViewLoad, Mode=OneWay}"
                Source="{x:Bind ViewModel.Source, Mode=OneWay}" />
        </StackPanel>
    </Grid>
</UserControl>
